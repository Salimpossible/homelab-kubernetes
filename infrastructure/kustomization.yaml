apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Infrastructure resources included in root kustomization.yaml
# Note: ingress/, monitoring/, network-policies/, nfs-provisioner/ are optional/planned features.
# Enable them when needed by adding to this resources list and root kustomization.yaml.

resources:
- namespaces/namespaces.yaml
- storage/storageclass.yaml
- storage/shared-pvc.yaml
- cert-manager
