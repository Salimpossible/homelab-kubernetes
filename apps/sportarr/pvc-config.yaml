apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sportarr-config
  namespace: media
  annotations:
    description: "Sportarr database and settings"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
