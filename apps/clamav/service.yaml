apiVersion: v1
kind: Service
metadata:
  name: clamav
  namespace: media
  labels:
    app: clamav
spec:
  type: ClusterIP
  selector:
    app: clamav
  ports:
  - name: clamd
    port: 3310
    targetPort: 3310
