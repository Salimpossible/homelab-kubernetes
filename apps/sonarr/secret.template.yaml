apiVersion: v1
kind: Secret
metadata:
  name: sonarr-bootstrap
  namespace: media
type: Opaque
stringData:
  # Replace with a 32-char API key before applying local secret.yaml
  apiKey: REPLACE_WITH_SONARR_API_KEY
